CREATE TABLE users (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
    -- I THINK username SHOULD BE UNIQUE 
    username TEXT NOT NULL, 
    password TEXT NOT NULL, 
    email TEXT NOT NULL UNIQUE
);

CREATE INDEX id_index ON users(id);